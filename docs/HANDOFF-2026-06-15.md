# US-007 交接进度文档

> **生成时间**: 2026-06-15
> **当前 branch**: `worktree-homework-session`
> **远程**: `origin/worktree-homework-session`
> **当前 HEAD**: `e0e7eea` (Tasks 6+7 完成)
> **目的**: 把 US-007 登录态持久化的 partial 实现交接给下一会话/设备/协作者继续

---

## 总体进度

| 任务 | 状态 | Commit |
|---|---|---|
| US-006 (作业列表) | ✅ 完成 | `6c1f063` |
| US-007 spec + plan | ✅ 完成 | `b40cdd0` |
| US-007 Task 1: ErrorCode 加 3 枚举值 | ✅ 完成 | `6a7408b` |
| US-007 Task 2: SessionStore (atomic write + path traversal guard) | ✅ 完成 | `c73a5a6` |
| US-007 Task 3: SessionResult sealed | ✅ 完成 | `9d83bd0` |
| US-007 Task 4: SessionProbe | ✅ 完成 | `717f988` |
| US-007 Task 5+10: BrowserLifecycle 接口扩展 + FakeBrowser 适配 | ✅ 完成(合并) | `32d4053` |
| US-007 Task 6+7: BookingContext 字段 + Restore/PersistSessionStep | ✅ 完成(合并) | `e0e7eea` |
| **US-007 Task 8: CasLoginStep 加 sessionOk 跳过判断** | ⏳ 待办 | — |
| **US-007 Task 9: ChaoxingHomeworkClient 接新 steps** | ⏳ 待办 | — |
| **US-007 Task 11: PlaywrightBrowserAdapter 真实现 storageState** | ⏳ 待办 | — |
| US-007 Task 12-13: 全量验证 + build | ⏳ 待办 | — |
| US-007 Task 15: 更新 system-map.md / design-patterns.md | ⏳ 待办 | — |
| US-007 Task 16-18: Story / ADR-0008 / Trace | ⏳ 待办 | — |
| US-007 Task 19: 最终验证 | ⏳ 待办 | — |

**测试数变化**: 323 (US-006 完成) → 326 (Task 1) → 333 (Task 2,9 个) → 333 (Task 3) → 336 (Task 4,3 个) → 338 (Tasks 5+10,FakeBrowser 加方法,无新测试) → 346 (Tasks 6+7,8 个新测试)

**当前 mvn test 结果**: 346/346 通过,0 失败,0 错误。

---

## 关键设计决策(已锁定)

来自 `docs/superpowers/specs/2026-06-14-us-007-session-persistence-design.md`:

| 决策点 | 选择 |
|---|---|
| 架构落点 | A. 扩展 BrowserLifecycle 接口(2 方法)— 已实现 |
| 存储位置 | A. `~/.szu-agent/sessions/<username>.json`,POSIX 600 |
| 会话语义 | A. TTL 30 天 + 探针主动失效 |
| 接口签名 | A. `boolean importStorageState(Path)` / `void exportStorageState(Path)` |

---

## 已实现组件清单

### 接口 / 数据类型

| 文件 | 内容 |
|---|---|
| `src/main/java/edu/szu/agent/error/ErrorCode.java` | + SESSION_NOT_FOUND / SESSION_READ_FAILED / SESSION_WRITE_FAILED 三个枚举值 |
| `src/main/java/edu/szu/agent/browser/BrowserLifecycle.java` | + `importStorageState(Path)` + `exportStorageState(Path)` 共 12 方法 |
| `src/main/java/edu/szu/agent/client/session/SessionResult.java` | sealed: `Fresh()` / `Stale(String reason)` |
| `src/main/java/edu/szu/agent/client/step/BookingContext.java` | + `sessionOk` / `username` 字段 + 4 accessor |

### 业务实现

| 文件 | 内容 |
|---|---|
| `src/main/java/edu/szu/agent/client/session/SessionStore.java` | 路径解析 / 创建目录 / 原子写带 POSIX 600 / TTL 判定 / 删除。**username 白名单 `^[A-Za-z0-9_.-]+$` 防路径穿越**。 |
| `src/main/java/edu/szu/agent/client/session/SessionProbe.java` | navigate + isVisible 判定,捕 BookingException 视为 Stale |
| `src/main/java/edu/szu/agent/client/step/RestoreSessionStep.java` | 试 import + 探针验证,Fresh → ctx.sessionOk=true,Stale → 删旧文件 |
| `src/main/java/edu/szu/agent/client/step/PersistSessionStep.java` | 业务成功(homeworks 非空)→ export storageState,失败仅 log warn |

### 测试夹具 / Stub

| 文件 | 内容 |
|---|---|
| `src/test/java/edu/szu/agent/browser/FakeBrowser.java` | + 2 个 no-op 方法 + 4 个 introspection 字段(loaded/saved/loadedPath/savedPath) |
| `src/main/java/edu/szu/agent/browser/PlaywrightBrowserAdapter.java` | 2 个 stub 方法,**throw UnsupportedOperationException**(待 Task 11 替换) |

### 新增测试(8 + 9 + 3 + 5 + 3 = 28 测试)

| 测试类 | 数量 | 覆盖 |
|---|---|---|
| `ErrorCodeTest` (改) | +3 | 3 个新 ErrorCode 元数据 |
| `SessionStoreTest` (新) | 9 | 路径 / 权限 / TTL / 路径穿越拒绝 |
| `SessionProbeTest` (新) | 3 | Fresh / Stale(无 indicator)/ Stale(navigate 异常) |
| `RestoreSessionStepTest` (新) | 5 | Fresh / 文件缺失 / TTL 过期 / import 失败 / probe Stale |
| `PersistSessionStepTest` (新) | 3 | 成功 export / homeworks 空 / homeworks null |

---

## 接下来要做的(剩余 Plan Task 8/9/11/12+)

按 plan `docs/superpowers/plans/2026-06-14-us-007-session-persistence.md` 执行:

### Task 8 — CasLoginStep 头部加跳过判断

文件:`src/main/java/edu/szu/agent/client/step/CasLoginStep.java`
改动:在 `execute` 方法 `var account = ctx.account();` 之前加:

```java
if (ctx.sessionOk()) {
    log.info("Skipping CAS login, persisted state valid for {}",
        ctx.account() != null ? ctx.account().studentId() : "unknown");
    return null;
}
```

跑 `mvn test`,期望 346/346 仍全绿。Commit message: `feat(step): CasLoginStep skips when ctx.sessionOk() is true`

### Task 9 — ChaoxingHomeworkClient 接新 steps

文件:`src/main/java/edu/szu/agent/client/ChaoxingHomeworkClient.java`
- 新增字段 `SessionStore sessionStore` / `SessionProbe sessionProbe` / `Duration sessionTtl`
- 新增 7 参构造器(原 4 参保留向后兼容)
- 在 `list()` 方法内 `executeSteps()` 之前 `ctx.username(account.studentId())`

测试 `ChaoxingHomeworkClientTest` 加一个 `listWithSessionDependencies()` 测试。预期 +1 测试 → 347/347。

### Task 11 — PlaywrightBrowserAdapter 真实现

**最重的一个 task**。文件:`src/main/java/edu/szu/agent/browser/PlaywrightBrowserAdapter.java`

改动:
1. 加字段 `private BrowserContext context;`,加 import
2. `open()` 改为 `browser.newContext()` → `context.newPage()`
3. `close()` 加 `context.close()`
4. 替换 `importStorageState` stub 为真实现(读 JSON 文件,addCookies + addInitScript localStorage)
5. 替换 `exportStorageState` stub 为 `context.storageState(new BrowserContext.StorageStateOptions().setPath(file))`,catch 异常包成 `BookingException(SESSION_WRITE_FAILED, ...)`

详细代码见 plan Task 11。Commit message: `feat(playwright): BrowserContext wrapping + storageState import/export`

### Task 12-13 — 验证

```bash
mvn -q test                    # 期望 347+/347+ 全绿
mvn -q -DskipTests package     # 期望 BUILD SUCCESS
```

### Task 15 — 文档

更新 `docs/system-map.md` §1 拓扑加 `client/session/` 模块,§7 ADR 索引加 ADR-0008 行。

### Task 16-18 — Story / ADR / Trace

- `docs/stories/US-007-session-persistence.md`(参考 US-006 story 格式)
- `docs/adr/0008-session-persistence.md`(参考 ADR-0005 格式,记 4 个决策点 + Context/Consequences/实施细节)
- `harness-records/traces/20260615-HHMMSS-US-007.md`(总结实施)

### Task 19 — 最终验证

```bash
git status --short          # 期望空
mvn -q test                 # 最后跑一次确认
git log --oneline master..HEAD  # 应有 ~14 commits
```

---

## 关键约束(继续工作时不要破坏)

1. **archunit ADR-0005 D2**: log 字面量不能含 `password|pwd|secret|token|cookie|session|authorization|bearer`。当前所有新代码已遵守,继续 Task 8/9/11 时**避免**:
   - log 串里写 "session" / "cookie"(用 "state" / "auth")
   - log 串里写 "load session state" → 改成 "load persisted state"
2. **storageState 文件不能写入凭证以外**:Playwright 的 `storageState()` 输出 JSON 包含 cookie + localStorage,**不要**手写 JSON 拼装(Task 11 用 SDK 自带方法)
3. **每个 commit 跑 `mvn test` 全绿**:中间状态必须 ship-able,不能有半改文件留下
4. **Path traversal 已防**:Task 2 的 `USERNAME_PATTERN ^[A-Za-z0-9_.-]+$` 是核心保护层,Task 9 接 Client 时**直接传 `account.studentId()`**(SZU 学号本身全数字,天然合规),不要手动拼接子路径
5. **Tasks 5+10 已合并**(plan 原顺序错了,FakeBrowser `@Override` 必须等接口先扩),Task 11 把 PlaywrightAdapter 的 stub 替换为真实现就好

---

## 接手指引(给下一会话/协作者)

1. **拉代码**: `git pull origin worktree-homework-session`(注意是临时 branch 名,worktree 创建时生成的)
2. **启动开发环境**: 见 `docs/setup/windows-maven.md` 或 `docs/setup/mac-maven.md`(macOS/Linux 也同,只是路径换)
3. **环境就绪验证**: `mvn -q test` 应输出 346 通过
4. **接 Task 8 起**: 按本文档"接下来要做的"小节,或参考 `docs/superpowers/plans/2026-06-14-us-007-session-persistence.md` 详细 step
5. **遇到问题**: 看 `docs/superpowers/specs/2026-06-14-us-007-session-persistence-design.md` 第 3-5 节(架构 / 设计细节 / 错误处理)

---

## Git 历史快照

```
e0e7eea feat(session): add RestoreSession/PersistSession steps + context fields
32d4053 feat(browser): BrowserLifecycle gains import/exportStorageState (12 methods)
717f988 feat(session): add SessionProbe for post-import validation
9d83bd0 feat(session): add SessionResult sealed type
c73a5a6 feat(session): add SessionStore with POSIX 600 perms + 30d TTL
6a7408b feat(error): add SESSION_NOT_FOUND/READ_FAILED/WRITE_FAILED codes
b40cdd0 docs(plan): US-007 session persistence spec and plan
6c1f063 feat(homework): US-006 chaoxing LMS homework list query
59c6b99 docs(trace): record phase5-step3 refactor + SelectDateStep isVisible→click fix  ← master
```

8 个 commit 领先 master,worktree 干净,可立即续工或直接开 PR。
